import { ExchangeRateItem } from './exchange-rate-item.model';

describe('ExchangeRateItem', () => {
  it('should create an instance', () => {
    expect(new ExchangeRateItem()).toBeTruthy();
  });
});
